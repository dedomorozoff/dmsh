# Tasks — Batch 2

- `[ ]` 1. Stdin pipe — читать stdin в one-shot режиме, добавлять как контекст
- `[ ]` 2. `/retry` — повторный запрос с альтернативным подходом
- `[ ]` 3. `/stats` — статистика сессии
- `[ ]` 4. `/export` — экспорт последней команды в буфер обмена / файл
- `[ ]` 5. `/alias` — пользовательские алиасы
- `[ ]` 6. Multi-turn контекст разговора
- `[ ]` 7. Тесты и верификация
